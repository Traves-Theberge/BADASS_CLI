import { Box, Text } from 'ink';

const StreamDisplay = () => (
  <Box borderStyle="single" padding={1} flexGrow={1}>
    <Text>Live Stream/Output Area</Text>
  </Box>
);

export default StreamDisplay;
